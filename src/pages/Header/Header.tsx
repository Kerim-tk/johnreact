import './Header.scss';

export const Header = () => {
    return (
        <div>
        <header className="header desktop-only">
          <a href="work.html">Works</a>
          <a href="#">Blog</a>
          <a href="#">Contact</a>
        </header>
      </div>
    );
}